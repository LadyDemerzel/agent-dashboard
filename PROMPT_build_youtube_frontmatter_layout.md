Task: Adjust YouTube video detail markdown front matter rendering UX in /src/app/youtube-videos/[id]/page.tsx.

Requirements:
- Do NOT wrap front matter rows in a "Metadata" outer container/card.
- Render each key/value on the same horizontal line (key and value side-by-side), not key above value.
- Keep styling clean and readable in dark mode.
- Keep changes scoped to YouTube metadata rendering only.
- Run npm run build before finishing.

When fully complete, write RALPH_DONE to the done file instructed by the loop.
